version: 0.0
os: linux
files:
  - source: /
    destination: /opt/cicd-lab
file_exists_behavior: OVERWRITE

hooks:
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 300
      runas: root
  ValidateService:
    - location: scripts/cleanup.sh
      timeout: 300
      runas: root
